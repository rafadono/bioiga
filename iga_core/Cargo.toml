[package]
name = "iga_rust"
version = "0.1.0"
edition = "2021"

[lib]
name = "iga_rust"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29.0", features = ["extension-module", "abi3-py312"] }
rayon = "1.10.0"

